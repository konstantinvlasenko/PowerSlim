!include -c <OriginalMode.SuiteExceptions.ScenarioLibrary
!define Server     {localhost}
!define Inputs     {format=xml}
!define RootUrl    (!-http://-!${Server}:${FITNESSE_PORT})

!|script                                                                                                              |
|$URL=|eval|'${RootUrl}/PowerSlim.OriginalMode.SuiteFixes.SuiteIssue62.FirstThrowsExceptions?responder=test&${Inputs}'|
|eval |$results = Run-Test -url '$URL'                                                                                |
|check|eval|([regex]::Matches($results.testResults.result.content, "System.IO.FileNotFoundException" )).count  |>=40  |
|show |eval|$results.testResults.result.content                                                                       |

